# Control-plane deps, pinned to versions resolved in the .venv-nat (Python 3.13)
# environment that also hosts the NeMo Agent Toolkit. NAT is installed
# separately via: uv pip install "nvidia-nat[langchain]"
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
openai==2.44.0
httpx==0.28.1
python-dotenv==1.2.2
loguru==0.7.3
# In-process LangGraph agent pipeline (build + self-healing graphs) and the
# RCA tool-calling ReAct agent. langchain-community powers the SQLite/Redis
# LLM response cache. Redis is optional (install `redis` + a checkpoint/store
# backend only if CACHE_BACKEND/CHECKPOINT_BACKEND/STORE_BACKEND=redis).
langgraph==1.2.6
langchain-core==1.4.8
langchain-nvidia-ai-endpoints==1.4.2
langchain-community==0.4.1
# Tester agent: lints the generated backend (Black + flake8) and runs the
# LLM-generated pytest suite against it (FastAPI TestClient). The agent degrades
# gracefully (reports "skipped") if any of these are missing at runtime.
pytest==8.3.4
black==24.10.0
flake8==7.1.1
